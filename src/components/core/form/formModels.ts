export type BakedItemNameModel = {
    id: string
    title: string
  }

  export type BakedItemTypeModel = {
    title: string
  }

  export type BakedItemModel = {
    id: string
    name: string
    type: string
    topping: string
  }

